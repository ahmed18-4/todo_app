<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="10dp"
    android:elevation="20dp"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    app:cardCornerRadius="20dp"
    >
<androidx.constraintlayout.widget.ConstraintLayout
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:layout_margin="10dp"
    >
    <View
        android:id="@+id/left_guide"
        android:layout_width="5dp"
        android:layout_height="120dp"
        android:background="@color/blue"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        />
    <TextView
        android:id="@+id/todo_title_item_view"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="play bascket ball"
        android:textSize="30sp"
        android:textColor="@color/blue"
        android:textStyle="bold"
        android:layout_marginStart="5dp"
        app:layout_constraintStart_toEndOf="@id/left_guide"
        app:layout_constraintEnd_toStartOf="@id/check_done_button"
        app:layout_constraintTop_toTopOf="parent"
        />
<TextView
    android:id="@+id/description_item_view"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:text="important training before the next match"
    android:textSize="20sp"
    android:gravity="center"
    android:padding="5dp"
    android:layout_margin="7dp"
    android:maxLines="2"
    android:ellipsize="end"
    app:layout_constraintTop_toBottomOf="@id/todo_title_item_view"
    app:layout_constraintStart_toEndOf="@id/left_guide"
    app:layout_constraintEnd_toStartOf="@id/check_done_button"
    />
    <ImageButton
        android:id="@+id/check_done_button"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/ic_check_done"
        android:adjustViewBounds="true"
        android:background="@color/white"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        />
</androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>